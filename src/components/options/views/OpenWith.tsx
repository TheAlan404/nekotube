import { Stack } from "@mantine/core";
import { useContext } from "react";

export const OptionsOpenWithView = () => {
    return (
        <Stack w="100%">
            meow
        </Stack>
    );
};
